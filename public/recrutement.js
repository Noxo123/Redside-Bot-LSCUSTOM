const root=document.querySelector('#app');const id=location.pathname.split('/').filter(Boolean).pop();const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));async function api(url,opt){const r=await fetch(url,opt);const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Une erreur est survenue');return d}async function load(){try{const r=await api(`/api/public/recruitments/${encodeURIComponent(id)}`);if(r.status!=='open')throw Error('Ce recrutement est actuellement fermé.');const image=r.image?`url('${String(r.image).replace(/'/g,"%27")}')`:'';root.innerHTML=`<article class="card"><div class="cover" style="${image?`--image:${image}`:''}"><div><h1>${esc(r.title)}</h1><p>${esc(r.description)}</p></div></div><div class="content"><p class="intro">Bienvenue sur le formulaire officiel de recrutement <b>LS CUSTOM</b>. Remplis toutes les informations avec des données correspondant à ton personnage et à ton identité Discord.</p><form id="form"><div class="section-title">Identité du candidat</div><div class="grid"><div class="field"><label>Nom *</label><input name="nom" required maxlength="60" autocomplete="family-name" placeholder="Ex. Martinez"></div><div class="field"><label>Prénom *</label><input name="prenom" required maxlength="60" autocomplete="given-name" placeholder="Ex. Lucas"></div><div class="field"><label>ID RP *</label><input name="rp_id" required maxlength="30" inputmode="numeric" placeholder="Ex. 4821"></div><div class="field"><label>Âge *</label><input name="age" required type="number" min="15" max="99" placeholder="Ex. 24"></div><div class="field full"><label>ID Discord *</label><input name="discord_id" required maxlength="30" inputmode="numeric" placeholder="Ex. 123456789012345678"><span class="hint">Active le mode développeur Discord pour copier ton identifiant.</span></div><div class="field full"><label>Présentation *</label><textarea name="presentation" required maxlength="2500" placeholder="Présente ton personnage, ton parcours et toi-même…"></textarea></div></div><div class="section-title">Motivation</div><div class="field"><label>Pourquoi souhaites-tu rejoindre LS CUSTOM ? *</label><textarea name="motivation" required maxlength="3000" placeholder="Explique ta motivation, tes objectifs et ce que tu pourrais apporter à l'équipe…"></textarea></div><div class="section-title">Expérience</div><div class="field"><label>Expérience RP / mécanique *</label><textarea name="experience" required maxlength="2500" placeholder="Anciennes entreprises, postes occupés, connaissances mécaniques, expérience RP…"></textarea></div><button class="submit" id="submit">ENVOYER MA CANDIDATURE</button><p class="privacy">En envoyant ce formulaire, les informations sont transmises à l'équipe de recrutement de LS CUSTOM pour traitement de la candidature.</p></form></div></article>`;document.querySelector('#form').onsubmit=submit}catch(e){root.innerHTML=`<div class="card"><div class="success"><div class="icon">⚠️</div><h2>Recrutement indisponible</h2><p>${esc(e.message)}</p></div></div>`}}async function submit(e){e.preventDefault();const btn=document.querySelector('#submit');btn.disabled=true;btn.textContent='ENVOI EN COURS…';try{const body=Object.fromEntries(new FormData(e.target));const r=await api(`/api/public/recruitments/${encodeURIComponent(id)}/applications`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});root.innerHTML=`<div class="card"><div class="success"><div class="icon">✓</div><h2>Candidature envoyée</h2><p>Merci pour ta candidature. L'équipe LS CUSTOM va maintenant l'étudier.</p><div class="reference">Référence : <b>${esc(r.reference||'LSC')}</b></div></div></div>`}catch(err){const old=document.querySelector('.error');if(old)old.remove();const p=document.createElement('div');p.className='error';p.textContent=err.message;e.target.prepend(p);btn.disabled=false;btn.textContent='ENVOYER MA CANDIDATURE'}}load();
